<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!--  for adding internet access permission  -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--  for accessing network state permission (know when internet is disconnected or not)  -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Salude"
        tools:targetApi="31">
        <activity
            android:name=".features.splash_screen.view.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--  MUST add other activities here  -->
        <meta-data
            android:name="com.google.android.gms.auth.api.credentials.CredentialPickerConfig.EnableSmartLock"
            android:value="false" />
        <activity android:name=".features.main_screen.view.MainScreenActivity"></activity>
        <activity android:name=".features.auth_firebase.login.view.LoginAuthFirebaseActivity"></activity>
        <activity android:name=".features.auth_firebase.register.view.RegisterAuthFirebaseActivity"></activity>
    </application>

</manifest>